<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <p1>Joueur créé</p1>
        <ul>
        {% for p in player %}
            <li>{{ p.name }}</li>
        {% endfor %}
        </ul>
    <a href="new">créer nouveau joueur</a>
    </body>
</html>
